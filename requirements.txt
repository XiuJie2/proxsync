# PVE-NetBox Sync - Requirements

# Core dependencies
pynetbox>=6.0.0
proxmoxer>=2.0.0
requests>=2.25.0
urllib3>=1.26.0

# Configuration management
PyYAML>=5.4.0
watchdog>=2.0.0  # 配置热重载

# Database (SQLite is built-in, no extra needed)
# For PostgreSQL/MySQL instead of SQLite: sqlalchemy, psycopg2-binary, pymysql

# NetBox plugin dependencies
# Note: Django and other web框架 already provided by NetBox
django-rq>=2.4.0

# Production dependencies
gunicorn>=20.0.0  # if running standalone web service
supervisor>=4.2.0  # process management
